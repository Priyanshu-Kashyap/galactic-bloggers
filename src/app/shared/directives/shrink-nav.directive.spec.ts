import { ShrinkNavDirective } from './shrink-nav.directive';

describe('ShrinkNavDirective', () => {
  it('should create an instance', () => {
    const directive = new ShrinkNavDirective();
    expect(directive).toBeTruthy();
  });
});
